# Отчёт по проделанной работе

Дата: 24 августа 2026. Ветка `main`, 44 коммита.

Документ для контроля: здесь зафиксировано, что сделано, чем это
подтверждено и что осталось. Матрица готовности по пунктам ТЗ — в
[STATUS.md](STATUS.md), порядок размещения — в [DEPLOY.md](DEPLOY.md).

---

## 1. Где что лежит

| | |
|---|---|
| Приложение | https://igor-bond.github.io/wordschatz/ |
| Репозиторий | https://github.com/Igor-Bond/wordschatz |
| Облако | Firebase `wortschatz-f1ca2`, тариф Spark (бесплатный) |
| Правила доступа к данным | [firestore.rules](firestore.rules) — эталон для сверки с консолью |
| Хостинг | GitHub Pages, ветка `main`, папка `/` |

Netlify оставлен запасным вариантом: `netlify.toml` в репозитории, Pages
его не читает. Причина ухода — бесплатный план Netlify считает кредиты и
при их исчерпании останавливает сначала деплои, потом раздачу сайта.

---

## 2. Числа

| Показатель | Значение |
|---|---|
| Модулей JS | 33 файла, 10 826 строк |
| Ключей локализации | 461 × 3 языка (ru, uk, en), расхождений нет |
| Версия схемы базы | 6 |
| Версия кэша (`APP_VERSION`) | v43 |
| Версия формата резервной копии | 3 |
| Загрузка приложения | 900 КБ, 39 запросов |
| Стили Tailwind | 37 КБ собранного CSS |

Загрузка до работы над стилями — 1388 КБ и 42 запроса: 441 КБ из них
составлял компилятор Tailwind, работавший в браузере при каждом запуске.

---

## 3. Что сделано

Порядок — по областям, а не по времени. У каждого пункта указано, какой
дефект он закрывает: без этого список превращается в перечень слов.

### Учебный цикл и планирование

- Таблицы `cycles` и `dayPlans` читались, но никто их не создавал —
  план дня брал первые N слов из словаря. Теперь тема раскладывается по
  дням, план дня читается из раскладки.
- Смена дневной нормы пересчитывает оставшиеся дни темы, пройденные и
  начатый сегодня не трогает.
- Если снять галочки в предпросмотре, план недобирал слов. Появилась
  кнопка «Добрать»: догенерирует недостающие, исключая и словарь, и уже
  показанные слова, чтобы снятые не возвращались.

### Карточка слова

- Существительное показывает Plural, Dativ, Akkusativ; глагол —
  Präteritum, Perfekt, Konjunktiv II, Imperativ, Rektion и полное
  спряжение Präsens шестью формами; прилагательное — степени сравнения.
- Perfekt выводится как «hat genommen», а не «haben genommen»: именно
  эту форму спрашивает упражнение.
- Präsens приходит от модели объектом, а карточка читала старое строковое
  поле — у всех новых глаголов настоящее время не показывалось вовсе.
- Imperativ и Konjunktiv II Wiktionary отдаёт даром, а разбор их
  пропускал.
- Слово закреплено вверху карточки: при прокрутке оно уезжало первым, и
  на экране оставался пустой край его блока.
- Шапка приложения скрывается на время занятия — 66 px в пользу карточки.

### Освоенность и интервальное повторение

- Поле `mastery` копилось: каждая оценка прибавляла «оценка × 5», включая
  «Снова». Слово, забытое десять раз, набирало полсотни процентов.
  Ответы в упражнениях не учитывались вовсе.
- Теперь освоенность вычисляется из длины интервала SRS и точности
  последних восьми ответов, пересчитывается при каждом ответе.
- «Освоено» = интервал от 21 дня при точности не ниже 85%.
- Ответы во всех девяти типах заданий идут в историю слова.
- SRS: фазы заучивания, разброс интервала, потолок 365 дней, лёгкость в
  пределах 1.3–3.0.

### Задания

- Экзамен спрашивал перевод, артикль и форму глагола — три типа из
  девяти, со своей отрисовкой и своей проверкой ответов. Переведён на
  движок упражнений: те же девять типов, тот же журнал ошибок, которого
  у него не было. 160 строк дублирующей отрисовки удалены.
- Каждому слову на экзамене достаются два разных посильных задания.
- Тип задания на уроке выбирается по освоенности слова: от узнавания к
  воспроизведению.
- Артикль не спрашивается там, где он неизвестен; Rektion — где её нет;
  аудирование — без установленного немецкого голоса.

### Достоверность содержимого

- Карточки пишет языковая модель, и ошибку в роде или в Perfekt заметить
  было некому. Добавлена сверка с немецким Wiktionary — бесплатно, без
  ключа, прямо из браузера.
- Сверка идёт при генерации темы, в сканере и по кнопке в профиле.
  Неверные формы правятся до того, как слово попадёт в план.
- Пустые поля закрываются оттуда же, поэтому дозаполнение сначала идёт
  в Wiktionary и только потом к модели — за примерами и синонимами.
- У омографов (`der/die See`, `der/das/die Band`) на странице несколько
  блоков форм; нужный выбирается по роду карточки, а при неоднозначности
  не утверждается ничего.
- Полнота карточек считается и показывается числом.

### Данные и синхронизация

- Вход через Google, обмен с Firestore изменениями по `updatedAt`,
  разрешение конфликтов «побеждает последняя запись», мягкое удаление.
- Обмен запускался только при старте и по кнопке. Добавлены два повода:
  завершение урока и уход со страницы.
- Резервная копия включает словарь, темы, планы, статистику, профиль и
  переписку с репетитором. API-ключ в копию не попадает.
- История чата переехала в базу: раньше она жила в памяти вкладки и
  исчезала при перезагрузке. Хранятся последние 200 сообщений, в промпт
  уходят последние 20.

### Приложение и размещение

- PWA: офлайн-режим, кнопка установки и проверка, какое из условий Chrome
  не выполнено — на телефоне DevTools нет, иначе причину не увидеть.
- Озвучка выбирает немецкий голос явно и объясняет, если его нет: раньше
  система молча читала немецкие слова русской фонетикой.
- Стили собираются заранее автономным Tailwind, Node не нужен.
- Три языка интерфейса, формы множественного числа через `Intl`.
- Все диалоги вместо `alert`, история ошибок, диагностика ключей ИИ.

---

## 4. Чем подтверждено

Проверки выполнены в браузере на собранном приложении 24 августа.

### Экраны и задания

Все шесть экранов и все десять типов заданий отрисовываются без ошибок
в консоли:

| Экран | План, Тема, Сканер, Комната, Чат, Профиль |
|---|---|
| Типы заданий | перевод DE→RU и RU→DE, ввод по-немецки, поиск пар, артикль, форма глагола, Rektion, пропуск в тексте, сборка предложения, аудирование |

### Интервальное повторение

Цепочка оценок для нового слова:

```
Хорошо → 0 д (заучивание)   Легко  → 10 д, лёгкость 2.65
Хорошо → 1 д (повторение)   Снова  → 5 д, возврат в заучивание
Хорошо → 3 д                Хорошо → 0 д
```

### Освоенность

| Состояние | Освоенность |
|---|---|
| Новое слово | 0% |
| Интервал 7 дней | 55% |
| Интервал 21 день | 80%, считается освоенным |
| Интервал 20 дней | не считается освоенным |
| 2 верных из 8 последних | помечается слабым |

### Серия дней

Занимался сегодня — 7; занимался вчера — 7 (серия жива); пропустил день —
0, и ноль записывается в базу, чтобы серия не воскресла.

### Сверка с Wiktionary

Подставленные ошибки ловятся на живых статьях:

| Что подставлено | Результат сверки |
|---|---|
| `das Zeit` | род: das → die |
| `dem Student` | дательный: dem Student → dem Studenten |
| `du nehmst` | Präsens du: nehmst → nimmst |
| `gehen` + `haben` | вспомогательный: haben → sein |
| `die See` (море) | расхождений нет — формы взяты из женского блока |
| Верная карточка | расхождений нет |
| Несуществующее слово | статьи нет, карточка не тронута |

### Резервная копия

Полный круг: 1 слово, 2 темы, 1 сообщение чата → экспорт (формат 3, API-ключа
внутри нет) → очистка → восстановление. Слово, сообщение и связь слова с
темой на месте. Мягкое удаление: слово исчезает из выдачи, но остаётся в
таблице, чтобы удаление доехало до облака.

### Приложение

На живом сайте: 8 условий установки Chrome из 9 выполнены (девятое —
предложение установки от самого браузера, оно появляется по его
усмотрению), service worker управляет страницей, немецкий голос найден.

---

## 5. Что осталось

Отложено по договорённости, работа не начиналась:

| Что | Почему отложено |
|---|---|
| §41 Уведомления | требует разрешения и сервера push либо локальных напоминаний |
| §6 Склонение прилагательных | таблица на 48 клеток нечитаема на телефоне |
| §10 Загрузка фото | разбор изображения в коде готов, нет интерфейса выбора файла |
| Слабые существительные отдельным заданием | форма уже видна на карточке |
| Тексты с пятью пропусками | сняты вместе с перетаскиванием |
| Vite | не нужен: главная выгода получена автономным Tailwind без Node |

Сняты с плана по договорённости: проверка произношения через микрофон,
отдельный месячный контроль, тематические возгласы ИИ, drag & drop.

---

## 6. Рекомендации

По убыванию пользы. Оценки трудоёмкости грубые.

### 1. Синонимы и антонимы из Wiktionary — полдня

Модель их часто не даёт, и в карточке остаются пустые строки. В статье
Wiktionary есть разделы `{{Synonyme}}` и `{{Gegenwörter}}`. Разбор
сложнее, чем у таблиц форм (свободный текст со ссылками), но выполним и
бесплатен. Это закроет самую заметную дыру в карточке.

### 2. Транскрипция IPA — час

Wiktionary отдаёт её в разделе `{{Aussprache}}` строкой
`{{Lautschrift|ˈatmən}}`. Для слов вроде `Chemie`, `Journalist`, `Orange`
произношение неочевидно, а голос есть не на каждом устройстве.

### 3. Уровень слова и тема на карточке — полчаса

Поля `level` и `topic` уже заполняются и хранятся, но нигде не
показываются. Чистое отображение, без единого запроса.

### 4. Значок сверки на карточке — полчаса

В словаре он есть, на карточке нет. Видеть «формы проверены» во время
учёбы полезнее, чем в списке.

### 5. Локальные уведомления — день

Полноценные push требуют сервера, но напоминание «план на сегодня не
пройден» можно показать через Notification API, пока приложение открыто,
плюс запись в календарь. Это половина §41 без инфраструктуры.

### 6. Загрузка фото — полдня

Расход ключа оказался мал: одно фото ≈ 4–5 тысяч токенов и один запрос,
против 16 тысяч и шести запросов на генерацию темы. Плитки Gemini
относительны размеру, поэтому снимок на 12 Мп стоит столько же, сколько
картинка 1024×768. Перед отправкой стоит уменьшить изображение до
~1024 px: на токены это не влияет, но упаковка станет в десять раз легче.

### 7. Автоматические тесты — два дня

Сейчас всё проверяется вручную в консоли браузера. Проверок накопилось
столько, что имеет смысл оформить их в набор, который прогоняется одной
командой. Без Node это HTML-страница с тестами, открываемая в браузере.

---

## 7. Ограничения, о которых стоит помнить

- **Сверка не ловит ошибки в омографах.** Если род карточки не совпал ни
  с одним блоком статьи или блоков с таким родом несколько, не
  утверждается ничего. Испортить верную карточку хуже, чем пропустить
  неверную.
- **Перевод не проверяется.** Wiktionary сверяет формы, но не значение.
  Ошибка вида «der Tisch — стул» пройдёт мимо.
- **Чат не синхронизируется в облако.** Firestore считает каждый
  документ, а 200 сообщений на устройство — заметная доля бесплатной
  квоты, потраченная не на слова. Переписка переносится файлом копии.
- **Стили нужно пересобирать** после добавления новых классов Tailwind:
  `tools/build-css.ps1`, затем закоммитить `css/tailwind.css`. Классы,
  собираемые подстановкой, перечислены в `safelist`.
- **Данные привязаны к домену.** IndexedDB у каждого адреса свой; при
  переезде словарь переносится входом в Google или файлом копии.
- **Обновление доезжает не мгновенно.** GitHub Pages держит файлы на своей
  раздаче около десяти минут: сразу после push сайт какое-то время отдаёт
  прошлую версию. Проверять по `APP_VERSION` в `sw.js`, а не по ощущениям.

---

## 8. Как проверить самому

```
# запустить локально
powershell -ExecutionPolicy Bypass -File .\tools\serve.ps1
# затем открыть http://localhost:8080/

# пересобрать стили
powershell -ExecutionPolicy Bypass -File .\tools\build-css.ps1
```

В приложении:

- **Профиль → Полнота карточек** — сколько форм не хватает, кнопка
  дозаполнения.
- **Профиль → Сверка с Wiktionary** — сколько слов сверено и где
  расхождения; в словаре есть фильтр «Расхождения».
- **Профиль → Установить приложение → Почему не устанавливается** —
  разбор всех условий Chrome по очереди.
- **Настройки → Проверить подключение** — какой ключ ИИ отвечает, а какой
  упёрся в квоту. Ключи в отчёте замаскированы.
- **Настройки → Информация** — версия кэша, объём данных, модель, голос.
