# Тестирование и аудит

Итоговая проверка версии 2.1 (кэш v70), 24 августа 2026. Первый аудит
лежит в `AUDIT.md` и относится к версии 1.7 — с тех пор изменилось
многое, поэтому проверка проведена заново и целиком.

Что подменялось: только генерация набора слов языковой моделью. Ключа
Gemini у меня нет и быть не должно. Всё остальное работало по-настоящему,
включая сетевые запросы к Wiktionary.

---

## 1. Числа

| Показатель | Значение |
|---|---|
| Модулей JS | 46 файлов, 12 227 строк |
| Крупнейший модуль | `modules/exercises.js`, 972 строки |
| Автоматических проверок | 285 в 20 наборах |
| Версия схемы базы | 6 |
| Загрузка без сжатия | 994 КБ, 53 запроса |
| Загрузка по сети (gzip GitHub Pages) | около 290 КБ |
| Готовность DOM | 139 мс |
| Ошибок в консоли за весь проход | 0 |

Сжатие даёт примерно 3,5 раза: `exercises.js` — 54 КБ против 13 КБ по
сети, `ru.js` — 38 КБ против 12 КБ. Цифра «994 КБ» — это то, что
разбирает браузер, а не то, что едет по сети.

---

## 2. Что проверено и работает

### Учебный цикл

| Проверка | Результат |
|---|---|
| Первый запуск, пять шагов | Отказ на пустом имени и ключе, рассказ про облако, настройки доехали |
| Генерация темы → сверка → предпросмотр → утверждение | 20 слов, дополнено 20 карточек |
| Сверка при генерации поймала подложенные ошибки | «der Tisch — стул», «hell — тёмный» |
| Урок: карточки, четыре оценки, практика | +81 XP, серия 1, экран итога |
| Все двенадцать режимов Комнаты | Запускаются с осмысленным содержимым |
| Экзамен | 20 вопросов, все 10 типов, 10 слов, разбор ошибок |
| Все одиннадцать отрисовщиков заданий | Каждый выдаёт разметку и рабочие обработчики |

### Данные

| Проверка | Результат |
|---|---|
| Мягкое удаление | Запись остаётся с `deletedAt`, из выдачи уходит |
| Метка изменения | Проставляется, синхронизация её видит |
| Резервная копия: выгрузка → восстановление | 5 из 5, ключа ИИ в файле нет |
| Словарь на 2000 слов | 951 узел вместо 38 000, 7 мс вместо 328 |

### Оболочка

| Проверка | Результат |
|---|---|
| Манифест | `fullscreen` с запасным `standalone`, 4 иконки, 2 маскируемых, тёмные цвета |
| Маскируемые иконки | Содержимое 52–53% ширины при допустимых 80% |
| Раскладка 360×800 | Нет горизонтальной прокрутки и вылета за край |
| Меню | `fixed`, зазор до низа окна 0 на всех экранах |
| Строки интерфейса | Ни одного сырого ключа на пяти экранах |
| Ошибки ключа ИИ | Понятное сообщение в сканере, чате, рассказе; ввод не блокируется |

### Апострофы

Отдельная строка, потому что это была самая долгоживущая ошибка:
«geht's» и «м'ясо» ломали кнопки, и приложение молчало. Сейчас слово с
апострофом доходит до задания целым и обратно, в словаре открывается и
удаляется, украинский перевод с апострофом отображается.

---

## 3. Оценка по современным меркам

### Пирамида проверок

Основание живое: 285 проверок на чистой логике, прогоняются одной
командой без Node и без сети. Они уже поймали шесть настоящих ошибок —
правило выпадения «e» (teuer → teuerer), пропущенное -n в дательном
множественного, видовую пару «убирать/убрать», двойной зачёт ответа в
сборке предложения и потерянный порядок по частоте.

Средний слой появился: шесть сквозных проверок гоняют путь целиком —
слово из словаря до задания, ответы по интервалам, старая запись через
все миграции до сегодняшнего плана, частотность до порядка в плане,
круг резервной копии, мягкое удаление. Идут по отдельной базе, словарь
живого человека они не видят.

Именно они и нашли, что порядок по частоте до плана не доезжал: обе
части по отдельности были правы, а вместе — нет. Ровно та ошибка, ради
которой средний слой и заводят.

Покрыто 18 модулей из 46. Непокрытое — это отрисовка (её сложно
проверять без браузерного драйвера) и `ai.js` (нужен ключ).
`core/scheduler.js` из рискованного непокрытым больше не числится:
дневной план теперь проходят сквозные проверки — и первую же ошибку
в нём нашли именно они.

### Доступность

Слабое место, но уже не провал. Что изменилось за этот заход:

- появилась область `aria-live` — экранный чтец объявляет смену экрана,
  верный и неверный ответ, последнее задание в уроке. Без неё он молчал:
  приложение перерисовывает содержимое целиком, а такие перерисовки чтец
  не читает;
- безымянные кнопки получили подписи (`aria-label`), в том числе
  переводимые через `data-i18n-aria` — вместе со сменой языка меняется
  и то, что слышит чтец;
- немецкие слова и примеры помечены `lang="de"`: чтец переключает
  произношение и не читает `Waschmaschine` по-русски;
- значки, ничего не добавляющие к смыслу, скрыты `aria-hidden`.

Что осталось: клавиатурный порядок обхода никто не проверял на реальном
чтеце — только в разметке. Модальные окна не запирают фокус внутри себя.
Проверок вживую (VoiceOver, TalkBack) не проводилось, и без них
утверждать соответствие WCAG нельзя.

Честная оценка: уровень A по букве почти набран, но не подтверждён.
Называть приложение доступным по-прежнему рано — теперь уже не из-за
дыр в разметке, а из-за отсутствия проверки живым чтецом.

### Производительность

290 КБ по сети и 139 мс до готовности DOM — хороший результат, особенно
после отказа от компилятора Tailwind в браузере. Узкие места убраны там,
где они были заметны: словарь рисуется порциями, поиск ждёт паузы в
наборе, снимок ужимается перед отправкой.

Что осталось: код не минифицируется и не делится на части. 675 КБ
собственного JS разбираются целиком при каждом холодном старте, включая
модули, которые в этом сеансе не понадобятся. Пока это 139 мс и
незаметно; при удвоении кода станет заметно.

### Безопасность и приватность

| Что | Состояние |
|---|---|
| Ключ ИИ в разметке | Нет |
| Ключ в резервной копии | Нет |
| Ключ на экране настроек | Скрыт, показывается по кнопке |
| Подстановка содержимого в код обработчика | 4 места, все с фиксированными значениями |
| Правила Firestore | Доступ только к своим данным по uid |
| Данные пользователя | Только на устройстве и в его облаке |

Класс ошибок с подстановкой в код закрыт: данные лежат в
`data`-атрибутах, где апостроф — обычный символ. Оставшиеся четыре
подстановки передают перечисления (`de`/`ru`, части речи, статусы), где
апострофа быть не может.

### Пользовательский опыт

Что сделано хорошо:

- **Приложение объясняет себя.** Раздел «Информация» показывает версию,
  кэш, объём данных, найденный голос; есть проверка подключения к ИИ,
  полнота карточек, сводка сверки. Разбирать «не работает» с телефона
  без этого пришлось бы гаданием.
- **Есть разбор для того, кто пришёл впервые.** «Как пользоваться» —
  девять свёрнутых разделов в профиле: с чего начать, где что искать,
  как считается расписание, что настроить сразу, частые вопросы. Числа
  в нём живые, из базы, а у описанных экранов есть кнопка перехода —
  справка не рассказывает, где кнопка, а приводит к ней. До этого
  непонятное лечилось точечно: отдельным экраном про облако в мастере,
  объяснением серии по нажатию. Каждая такая заплата и была признаком,
  что общего места для объяснений нет.
- **Отказы понятны.** Недействительный ключ называется недействительным
  ключом, а не «ошибкой сети». Напоминание честно пишет, чего не умеет.
  Сверка молчит там, где не уверена.
- **Выбор оставлен человеку** там, где решение не однозначно: слияние
  против замены при импорте, подозрение в переводе вместо
  автоисправления. Полноэкранный режим из этого списка выбыл:
  выключателя больше нет, режим задаёт манифест — страница из
  заданного им режима выйти всё равно не может.

Что остаётся неудобным:

- **Нельзя вернуться назад** в первом запуске. Опечатку в имени теперь
  можно поправить в настройках, но сам мастер идёт только вперёд.
- **Чёрная полоса сверху на iPhone.** Строка состояния там задана меткой
  `black`: окно встаёт под ней и доходит до низа экрана. Полный экран
  дала бы `black-translucent`, но с ней iOS отдаёт окно на 59 точек
  короче экрана и прижимает его к верху — меню повисает над краем, а
  нижние 59 точек перестают нажиматься. Пройдено пять раз; выбрано меню
  на своём месте.
- **Отсутствие часов на Android.** Обратная сторона полноэкранного
  режима: системные панели прячутся целиком, вместе с часами и зарядом.
  Размен сделан осознанно и по прямой просьбе. Одно следствие учтено:
  полосу жестов у нижнего края система оставляет за собой, поэтому
  содержимое меню в этом режиме поднято на 24 точки.

### Методика обучения

Одна из трёх оценок `REVIEW.md` снята: слова теперь **взвешены по
частотности**. Набор раскладывается от ходовых к редким по списку
OpenSubtitles, и в дневной план они попадают в том же порядке — сперва
то, что встретится завтра же. Две другие остаются: устной речи нет,
грамматика остаётся на уровне слова. Приложение хорошо учит узнавать
и писать; говорить оно не учит.

#### Нагрузка и темп

Разбор арифметики расписания вскрыл то, чего не видно глазами, и цену
ошибки в самом разборе тоже.

Сначала я посчитал на бумаге: интервалы растут как 1, 3, 8, 20, 50, 125,
313 дней, карточек с интервалом I накапливается I×норма, значит каждая
ступень даёт ровно «норму» повторений в день, а ступеней к году семь.
Вышло 7–8 норм, и потолок подняли до восьми.

Расчёт был верным и неполным. Он не учитывал двух вещей: лестницу
заучивания (новое слово проходит два коротких шага — это две лишних
ежедневных ступени) и провалы (забытое слово лезет по лестнице заново;
при точности 90 % это треть всей нагрузки). Прогон на 365 дней с
подменой часов дал **13,2–13,4 нормы**; потолок взят с запасом — четырнадцать.

Отсюда исправления:

- **потолок повторений** — четырнадцать норм вместо прежних пяти. Число
  измерено, а не выведено, и проверка держит его прогоном, а не
  утверждением;
- **урок вдвое короче**: слово проходит его один раз, а не два. Раньше
  каждое слово получало карточку, а через несколько минут упражнение;
  второе извлечение шло почти сразу за первым и прочности не добавляло,
  потому что её даёт разнесённость во времени. Карточку теперь получают
  трудные слова — им полезно пересмотреть запись целиком, — остальные
  идут через упражнение;
- **норма новых слов подчинена факту**: три дня подряд с переносами —
  ступень вниз, с объяснением на экране плана. Прежде это были два
  независимых числа, и человек расплачивался за свой выбор через
  полгода, когда связь причины со следствием уже не видна.

Заодно исправлено главное расхождение с наукой: расписание двигала
только самооценка на карточке, а ответ в упражнении — объективный и
потому более достоверный — не влиял на сроки вовсе. Теперь двигает и он.

Это, в свою очередь, вскрыло храповое колесо: ответ в упражнении
двоичен, «Легко» взять неоткуда, а каждая ошибка роняла лёгкость на
0,2 без пути назад. Доля трудных слов росла 8 % → 23 % → 52 % и дальше,
пока трудными не стали бы все. Теперь верный ответ лёгкость понемногу
возвращает — вчетверо медленнее, чем ошибка её роняет.

Ярлыки времени пересчитаны по измеренному спросу: 5 слов ≈ 15 минут,
10 ≈ 27, 15 ≈ 40, 20 ≈ 55. Это установившийся режим, а не первые недели.

#### Разнообразие заданий

Десять типов заданий — это на весь словарь, а не на одно слово. У
конкретного слова выбор куда уже: артикль, управление, форма глагола и
окончание прилагательного исключают друг друга по части речи, а
«вставить слово» и «собрать предложение» требуют примера в карточке.
Существительное без примера получало на этапе закрепления ровно два
типа, фраза — один.

Поправлено с двух сторон: в закрепление добавлен `match_pairs`, который
не требует от карточки ничего, и один и тот же тип больше не выпадает
дважды подряд. Но главный рычаг у владельца словаря: заполненный пример
удваивает выбор, а дозаполнить карточки можно одной кнопкой в профиле.

Зато закрыта главная опасность такого приложения — **уверенное обучение
неправильному**. Формы сверяются с Wiktionary, перевод сверяется и
помечается подозрением, спорные случаи не трогаются вовсе.

---

## 4. Заключение

Приложение готово к ежедневному использованию. Оно проходится насквозь
без единой ошибки в консоли, делает всё заявленное, честно ведёт себя
при отказах и объясняет своё состояние. Сорок восемь модулей и
285 проверок для проекта, написанного под одного человека, — это выше
обычной планки.

Три вещи, названные здесь главными, сделаны — в том порядке, в каком
были названы:

1. **Сквозные проверки.** Шесть сценариев гоняют путь целиком. Первая
   же находка оправдала работу: порядок по частоте не доезжал до
   дневного плана, и поодиночке обе части были правы.
2. **Доступность до разумного минимума.** `aria-live` на результат
   ответа и смену экрана, подписи безымянным кнопкам, `lang="de"` на
   немецком. Осталось подтвердить живым чтецом — см. раздел выше.
3. **Частотность слов.** Список на 14 869 слов, попадание 94 %,
   подгружается по требованию. Самое дорогое по трудоёмкости и самое
   ценное для результата.

Решено по итогам разбора: остаёмся на русском — хранение переводов по
языкам снято с плана; полноэкранный режим задан в манифесте; уведомления
сняты целиком — push требует планировщика снаружи, а напоминание внутри
приложения срабатывало только при открытом приложении, то есть когда
напоминать не о чем. Код, расписание и документация удалены.

Не планируется без отдельного решения: минификация и разделение кода.
