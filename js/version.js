/**
 * Версия приложения — одно число на весь проект.
 *
 * Показывается в «Настройки → Информация» под названием. Раньше стояла
 * прямо в разметке и молча устаревала; здесь её видно и правится она
 * в одном месте.
 *
 * ПРАВИЛО: правится при каждом изменении, которое доезжает до пользователя.
 * Мелкая правка — третье число (1.0.1), заметная функция — второе (1.1),
 * первое меняется, когда меняется сам продукт.
 *
 * Не путать с APP_VERSION в sw.js: та отвечает за сброс кэша и поднимается
 * при любом изменении файлов, даже невидимом.
 *
 * 4.0 — выпуск. Первое число сменилось не из-за одной большой функции, а
 * потому что закрыт весь список: все пункты ТЗ имеют состояние, снятое
 * снято по договорённости, документы сверены с кодом. Продолжение
 * возможно, но эта версия самодостаточна.
 */
export const VERSION = '4.0.1';
