/**
 * Общий объект профиля.
 *
 * Экран профиля вырос до полутора тысяч строк и держал в себе пять
 * несвязанных вещей: статистику с лигами, словарь с редактором, качество
 * карточек, облако и резервную копию. Разложены по файлам; собираются
 * обратно в `modules/profile.js`.
 *
 * Пустой объект здесь нужен, чтобы части могли звать друг друга
 * (`profile.renderDictionary()` из редактора, `profile.renderStats()`
 * после правки слова), не импортируя друг друга по кругу. Файл ничего не
 * импортирует сам, поэтому цикла не возникает, а к моменту первого вызова
 * объект уже наполнен.
 */
export const profile = {};
