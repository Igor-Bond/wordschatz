/**
 * Общий объект заданий.
 *
 * Файл `modules/exercises.js` дорос до 1277 строк и держал в себе четыре
 * несвязанные вещи: ход урока с выбором типа задания, отрисовку десяти
 * заданий, сборщик из кусков и проверку ответа с начислением опыта.
 * Разложены по файлам; собираются обратно в `modules/exercises.js`.
 *
 * Пустой объект здесь нужен, чтобы части могли звать друг друга
 * (`exercises.next()` из проверки ответа, `exercises.renderCurrent()`
 * из перехода дальше), не импортируя друг друга по кругу. Файл ничего
 * не импортирует сам, поэтому цикла не возникает, а к моменту первого
 * вызова объект уже наполнен.
 *
 * Так же устроен профиль — см. `modules/profile/shared.js`.
 */
export const exercises = {};
