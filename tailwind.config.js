/**
 * Конфигурация Tailwind для сборки статического CSS.
 *
 * До этого стили генерировал Play CDN — компилятор Tailwind весом 441 КБ,
 * работавший прямо в браузере при каждом запуске. Сборка заранее оставляет
 * от него килобайт пятнадцать готового CSS.
 *
 * Собирается автономным исполняемым файлом, Node не нужен:
 *   powershell -ExecutionPolicy Bypass -File .\tools\build-css.ps1
 *
 * Подробности — в docs/DEPLOY.md.
 */
module.exports = {
    content: [
        './index.html',
        './js/**/*.js'
    ],

    /**
     * Классы, которые сканер найти не может.
     *
     * Play CDN смотрел на живой DOM и видел любые классы; сборщик читает
     * исходники и ищет в них строки целиком. Шесть мест в коде собирают
     * класс из кусков — `text-${accent}`, `bg-${stepColor}/10` и подобные, —
     * и такие строки в исходниках не встречаются ни разу.
     *
     * Возможные значения подстановок:
     *   accent    — purple-400 (урок и Комната), red-500 (экзамен)
     *   stepColor — blue-400 (повторение), green-400 (новые слова)
     *
     * Если добавится новый цвет шапки, его нужно дописать сюда, иначе
     * заголовок останется без цвета.
     */
    safelist: [
        'text-purple-400', 'bg-purple-400/10', 'border-purple-400/20',
        'text-red-500', 'bg-red-500/10', 'border-red-500/20',
        'text-blue-400', 'bg-blue-400/10', 'border-blue-400/20',
        'text-green-400', 'bg-green-400/10', 'border-green-400/20'
    ],

    theme: { extend: {} },

    plugins: []
};
